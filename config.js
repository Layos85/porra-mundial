/* ============================================================
   Configuración de Supabase para la Porra del Mundial.

   Rellena estos dos valores con los de TU proyecto de Supabase:
   Supabase → Project Settings → Data API (o "API")
     · URL  →  Project URL          (ej: https://abcd1234.supabase.co)
     · KEY  →  anon / public key     (clave larga "anon public")

   La clave "anon public" es segura para publicarla en la web.
   Mientras estén en blanco, la app mostrará la pantalla de configuración.
   ============================================================ */
window.PORRA_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
